import mongoose, { Types } from "mongoose";

export const aboutUs = {
    title: "about us",
    bannerURL: "imageUrl",
    secondaryTitle: "secondary title",
    list: [],
};

export const tempID = new mongoose.Types.ObjectId();